/**
 * Car rental listing on Armada — charged per vehicle per week (JMD).
 */
export const CAR_RENTAL_LISTING_FEE_PER_VEHICLE_WEEK_JMD = 1500;

/** One billing week in days (used when extending subscription). */
export const CAR_RENTAL_LISTING_WEEK_DAYS = 7;
